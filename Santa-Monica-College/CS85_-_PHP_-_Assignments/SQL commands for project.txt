To do for the assignment


1.  Create database tables with the following commands:

TABLE 1
sportsfans


create table if not exists sportsfans (userid int(6) unsigned NOT NULL... AUTO INCREMENT PRIMARY KEY,
name varchar(40), registered_when timestamp);

create table messages (messageid int(6) unsigned NOT NULL...AUTO INCREMENT PRIMARY KEY,
team_thread varchar(40), thread_topic varchar(50), message text);

